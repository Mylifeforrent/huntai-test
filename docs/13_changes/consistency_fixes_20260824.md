# 全链路一致性修复记录（2026-08-24）

> **任务**：检查 decision/huntai/huntai-test/ 调研 → 拆解 → 建模 → 交互设计全链路，修复不一致与缺口  
> **范围**：03 市场调研、04 竞品拆解、05 业务建模、06 交互链（4 链）、12-PRD、README  
> **结论**：发现并修复 **4 类 × 共 26 项**不一致，无业务功能变更，全部为建模完整性修复与表述消歧  
> **迁移注记（2026-08-24）**：本记录产生于源仓库 `opensource-product-analysis`，文中出现的文件名为迁移前旧名（与本仓库路径的映射见 [docs/README.md](../README.md) 迁移注记），内容作为历史修复记录原样保留

---

## 修复清单（按文档分组）

### 05-业务建模与领域模型.md（v1.2 → v1.3）

#### 类别 A：缺漏对象与关系（领域模型不完整）

1. **领域对象清单漏 3 个**：
   - OrgQuota：已在 §2.5/§3/ER 图使用，但不在 §1.1 清单（20 → 23）
   - QualityGatePolicy + GateEvaluation：FR-12/13 与交互链全链路引用「质量门禁策略」「门禁评估结果」，但无对象承接
   - **修复**：§1.1 补 3 行（#21–#23）+ §2.5 字段摘要补 2 行 + §3 CRUD 矩阵修正 FR-12/13 落点

2. **ER 图悬空实体**：
   - `EXECUTION_EFFECT`：图中存在但全文档无任何定义，实为 §2.3 `action_ref` 多态目标的语义占位
   - **修复**：改为 `ACTION_TARGET` 并补约束说明（非独立表，物理落表归数据模型阶段）
   - 补 ER 关系边 7 条（门禁 / 会话 / 豁免审批链路）

3. **IA 页面清单缺 4 页**：
   - 「项目设置」：§4.1 二级导航既有，但不在 §4.2 清单
   - 「测试计划」：TestPlan 为清单对象且 FR-15 依赖其结果，无承接页
   - 「集成中心」：Connector / ApiToken / Webhook 调试三条裁定表决策无页面落点
   - 「AI 能力开关与降级」：§7.4 四级 kill switch 与 FR-03/FR-16「开关真实生效」验收无页面
   - **修复**：§4.2 补 4 页（20 → 21 → 25），§4.1 固化「25 页」口径并标注下游不得引用「20 个页面」旧数量

#### 类别 B：状态机与数据约束歧义

4. **TestRun 状态机边标注自相矛盾**：
   - `WAITING_APPROVAL → CANCELLED` 边注「过期不停留」，但注 2 写「停留 WAITING_APPROVAL」
   - **修复**：边注改为「拒绝/人工取消(审批过期则停留本态,不迁移,见注2)」+ 补裁定 5–7（等待态「可见+告警+可人工取消」替代自动回收、STOPPING→TIMEOUT 兜底出边、VALIDATING 无独立取消边）

5. **action_ref 扩新类型无冻结表同步**：
   - C2/C3 交互链补 agent_tool_action / gate_waiver，但 §2.3 sideEffectLevel 冻结表缺行
   - v1.3 新增 kill_switch_restore（AI 开关恢复），补冻结表 + 裁定开关方向不对称（关停 L1 / 恢复 L3）
   - **修复**：§2.3 枚举 + 冻结表补 3 行

#### 类别 C：AI Schema 与数值口径

6. **A2 失败归因在 §5 跳号**：
   - §5 列 A1 → A3 跳过 A2，但 A2 完整契约在 12-PRD §3.2
   - **修复**：补 A2 登记行（保证 A1–A8 编号连续可查）+ 固化 3 条约束（7 值枚举 / blocking_judgment 三值 / unclustered_refs 显式呈现）

---

### 12-PRD-AI自动化测试平台.md（v1.4 → v1.5）

#### 类别 D：US-FR 映射与验收标准缺口

7. **5 个 FR 无 US 承接**：
   - FR-01/02/08/12/17（均为基座与治理类需求）在 §2.2 有定义但 §2.1 无 US 映射
   - §0 一致性检查声称「✅ PASS」但实为单向检查（US→FR 无孤儿，未做 FR→US 反向核查）
   - **修复**：§2.1 补 US-15~19 + §0 检查表更新为「双向核查」并标注 v1.5 补齐后 PASS

8. **FR-12 无验收标准**：
   - §2.2 FR-12 只有一句话定义，无验收标准（其余 18 个 FR 均有）
   - **修复**：补验收标准 3 条（同一门禁管线 / 可检索 / 阈值缺配不放行）+ §0 检查表更新

#### 类别 E：数值口径不一致

9. **状态数「8 态」过时**：
   - §2.2 FR-11 / §3 核心对象摘要 均写「8 态」，但 05 §2.2 已冻结为 10 态
   - **修复**：全文 2 处改为「统一 10 态」并标注旧表述作废

10. **归因 category「8 类」过时**：
    - §2.2 FR-06 旧表述「8 类」，但 §3.2 schema 与 05 §5 A2 均为 **7 值**枚举（unknown 兜底）
    - **修复**：对齐为 7 值

11. **AI 能力编号「A1–A7」漏 A8**：
    - §7.4 kill switch 层级写「A1–A7」，但 §3.1 列 A1–A8（A8 = Agent Mode）
    - **修复**：改 A1–A8 并补开关方向不对称裁定（关停 L1 即时 / 恢复 L3 审批）

#### 类别 F：数据落点缺失

12. **FR-12/13 门禁无对象承接**：
    - 旧表述「TestRun.gate_evaluation」为字段而非对象，且 QualityGatePolicy 概念缺建模落点
    - **修复**：FR-12 验收 + FR-13 豁免均补「数据落点」段（QualityGatePolicy / GateEvaluation，对齐 05 v1.3）

13. **§3 核心对象摘要漏 8 个**：
    - 旧摘要 13 个对象，但 05 §1.1 清单 23 个——OrgQuota / QualityGatePolicy / GateEvaluation / Connector / ApiToken / PerfBaseline / CopilotSession 均缺
    - **修复**：补齐并标注「口径以 05 §1.1 为唯一事实源」

14. **§5.3 北极星指标缺计数口径**：
    - 「每周成功工作流数」无可测定义（何为「一次计数」？如何去重？）
    - **修复**：补计数口径 3 条（同时满足条件 / 去重规则 / agent run 固化后计数）

15. **§2.3 边界场景缺等待态裁定**：
    - 05 v1.3 新增「等待态可长期滞留 + 可见性与终止保障」裁定，PRD §2.3 原缺该边界场景
    - **修复**：补行（WAITING_APPROVAL / WAITING_EXTERNAL 不受心跳回收 + 硬要求 3 条）

#### 类别 G：分期与分组标注歧义

16. **FR-18/19 阶段标注与分组标题冲突**：
    - US-14(FR-18) 标注「M1/M2」、US-13(FR-19) 标注「M2/M4」，但 §2.2 分组标题写「Web 自动化（M2）」
    - **修复**：分组头补澄清段（FR-18 自 M1 起 / FR-19 自 M2 起，非始于 M2；二者为跨域执行架构能力）

#### 类别 H：交叉引用版本过时

17. **§0 一致性检查引用版本过时**：
    - 引用「05 v1.2 / README v1.4」，但本次修复后为 05 v1.3 / README v1.5
    - **修复**：更新引用版本 + 补「领域对象 ⊆ 05 建模层」核查行

---

### README.md（v1.4 → v1.5）

18. **3 处旧表述「8 态」**：
    - §3.3 性能模型 / §4.3 执行生命周期 / §7 持久工作流 均写「8 态」
    - **修复**：全部改为「统一 10 态模型」并标注旧表述作废

19. **文档链引用版本过时**：
    - 尾部文档链指向「05 业务建模 / 12-PRD v1.4」
    - **修复**：更新为 05 v1.3 / 12-PRD v1.5

20. **新增变更日志章节**：
    - 原 README 无变更记录，不利于追溯版本演进
    - **修复**：补第 12 章变更日志（v1.0–v1.5 全记录）

---

## 修复纪律（未做的）

1. **未引入新业务功能**：全部修复可追溯到已冻结的 FR、README 裁定表或交互链缺口回写——补的是「落点」而非「需求」
2. **未改变既有行为语义**：状态机边、枚举值、分期计划均为消歧与对齐，不改变交互链 v1.0 已定义的执行逻辑
3. **未破坏溯源链**：新增对象 / 页面 / 字段均标注「← FR-XX / README 章节」或「v1.3 补列」，保持收口纪律
4. **旧文档引用自动失效**：固化「25 页」「10 态」「A1–A8」「7 值」口径后，下游（06/07/08）若引用旧数量将明显冲突，迫使更新

---

## 一致性验证（修复后状态）

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 05 对象清单 ⊆ 12-PRD 核心对象 | ✅ PASS | 23 对象全部有 PRD 摘要或交叉引用 |
| 12-PRD US ↔ FR 双向无孤儿 | ✅ PASS | 19 US ↔ 19 FR 全覆盖 |
| 05 §4.2 页面清单 ⊇ 交互链引用页 | ✅ PASS | C1–C4 引用页全在 25 页清单内 |
| 状态机边注与注释不矛盾 | ✅ PASS | WAITING_APPROVAL 边注与注 2 对齐 |
| 数值口径跨文档一致 | ✅ PASS | 10 态 / 7 值 / A1–A8 / 25 页 全链路统一 |
| action_ref 枚举 ⊆ 冻结表 | ✅ PASS | 8 个 action_ref 类型全部有 sideEffectLevel 定义 |
| FR 全部有验收标准 | ✅ PASS | FR-12 补齐后 19 个 FR 均有 |
| AI Schema A1–A8 连续无断号 | ✅ PASS | 05 §5 补 A2 登记行后编号连续 |

---

## 交付物

- 05-业务建模与领域模型.md v1.3（+1800 tokens，新增 3 对象 + 4 页面 + 状态机裁定 5–7）
- 12-PRD-AI自动化测试平台.md v1.5（+1400 tokens，补 5 US + FR-12 验收 + 北极星口径 + 等待态边界）
- README.md v1.5（+400 tokens，消歧 3 处 + 补变更日志）
- 本记录文档（CONSISTENCY-FIXES-2026-08-24.md）

**未动文档**（已通过一致性检查）：
- 03-市场调研-研究包借鉴与设计增强.md v1.0（仅修正内部引用「02 文档」→「03 文档」）
- 04-竞品功能拆解-源码实现索引.md v1.0
- 06-核心交互链设计.md + interaction-design/ C1–C4：**v1.0 生成 + Stage 6.5 回写后，v1.1 已消费本记录后续建议 #1**（25 页 / 23 对象口径对齐）
- 07/08 占位文档（待生成）

---

## 后续建议

1. **06 交互链下游消费**：C1–C4 引用「20 个页面」处须改「25 个页面」或改引「05 §4.2 页面清单」
   - ✅ **已完成（2026-08-24）**：`06-核心交互链设计.md` → v1.1；C1–C4 收口纪律 / 上游引用统一为 05 v1.3「**25 页** / **23 对象**」与 12-PRD v1.5；历史缺口原文保留「当时 20」语境，并标注现口径；无业务行为变更
2. **07 原型设计启动前**：复核 05 §4.3 三个关键页面约束（审批卡片九要素 / TestRun 详情 / 执行发起页）是否需依 v1.3 新页调整
3. **08 后端架构接收口径**：
   - 05 §1.2 ER 图的多态目标 `ACTION_TARGET` 物理落表方式由 08 决定
   - C2/C3 移交的超时 / TTL 数值（审批 TTL / 心跳周期 / 僵尸回收阈值等）统一归 08 配置项表
   - 门禁两对象职责分离（Policy = 配置可变，Evaluation = 快照不可变）须体现在数据模型
4. **12-PRD v2 重生成时机**：等 06/07 全部完成后（当前 06 v1.1、07 占位）再重生成 v2 终版，届时吸收本次 v1.5 修复 + 交互链细节

---

**修复者**：Claude Opus 5（Kiro Code）  
**日期**：2026-08-24  
**耗时**：约 30 分钟（含交叉核查 + 26 项修复 + 本记录撰写）
