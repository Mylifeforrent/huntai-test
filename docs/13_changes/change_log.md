# 变更记录（change_log）

> 使用规则：任何对已沉淀设计资产或工程规范的修改，必须**先**在下方表格追加一行并获批准，再执行修改。
> 新记录在上（按时间倒序）。登记格式不完整视为未登记。

| 日期 | 变更对象（路径） | 变更摘要 | 原因 | 批准人 | 状态 |
|---|---|---|---|---|---|
| 2026-09-14 | `integration_hub.connectors.outbound_channels`（实现列） | Alembic 0026 增加 `outbound_channels` jsonb NOT NULL 默认 `[]`，承接 API-165/166 出站通知渠道配置；`data_model.md` §7.11 未列该列 | D3 切片需要持久化连接器出站渠道主备；实现侧补列并登记 | 用户本会话确认 D3 Plan | 已完成 |
| 2026-09-12 | `backend/scripts/mock_integrations.py`、`backend/scripts/seed_local_identity.py`（集成种子）；产品 loopback httpx 接线；`docs/00_setup/local-testing-guide.md`、`user-ui-guide.md`（§7 质量闭环正文）、`developer-guide.md`、`images/README.md`、`tutorial/integration-hub.html`；`images/7-*.png` | 本地集成 mock（loopback `127.0.0.1:8091`）+ 种子 4 Connector；产品在 `action_contract.base_url` 为 loopback 时 httpx 出站（Jira 写 / check-run / Release prepare）。教程：`user-ui-guide.md` **§7 逐步走查已写**；`images/7-*.png` **已拍并入库**。**不新增** US / FR / API / 页面 / 对象；Confluence 演示页**不是** `Connector.type` | 本地干净库可复现质量闭环而不接生产外部系统；用户本会话收口 | 用户（本会话明确要求收口并提交） | 已完成 |
| 2026-09-12 | `docs/00_setup/local-testing-guide.md`、`user-ui-guide.md`、`developer-guide.md`、`tutorial/*.html` | 按当前 `frontend/` / `backend/` 回写本地教程：探活改为 `/healthz` `/readyz`；补齐 `config.py` 必填键；更正 API-062 HTTP 200、幂等冲突 `HT-IDEM-001`、CAS `HT-VER-001`、校验 `HT-VAL-001`；更新行号与夹具；覆盖范围表对齐 M1–M3 已落地能力与本地边界。不改 US/FR/API/页面编号，不改冻结契约 | 用户本会话要求教程与当前前后端代码同步 | 用户（本会话明确请求更新 `docs/00_setup` 教程） | 已完成 |
| 2026-09-12 | 根 `AGENTS.md` §1；`docs/00_setup/project_rules.md` §2；`.env.example`；新增根 `docker-compose.yml`、`.github/workflows/ci.yml` | 仓库顶层白名单增补 `docker-compose.yml` 与 `.github/`，作为 Stage 12 构建部署产物的落点；`.env.example` 增补 `POSTGRES_PASSWORD` 键名（供 compose 注入 PostgreSQL 口令，**仅键名与说明、不填值**）。两个新文件只做编排与门控，不含业务规则；**不新增** US / FR / AC / API 编号、页面、领域对象或状态 | Stage 12 需要 compose 与 CI 的仓内落点：`project_rules.md` §5.3 规定 CI 由 Stage 12 引入且「CI 为绿」是判定标准，而 `AGENTS.md` §1 与 `project_rules.md` §2 的顶层白名单原为封闭列表，不含根 `docker-compose.yml` 与 `.github/`；按 R2 走「先登记 → 获批准 → 两处同改」流程 | 用户（本会话明确批准修订白名单并落地 compose + CI） | 已完成 |
| 2026-09-06 | `.env.example` | 仅注释修订：① `API_KEY`/`SECOND_API_KEY`/`JWT_SECRET`/`SESSION_SECRET`/`SENTRY_DSN` 注释改为「模板预留；当前后端 `backend/app/core/config.py` 不读取」；② `SENTRY_DSN` 补「留空则不上报」。键名与取值不变，不新增配置项 | 用户要求为每个配置项加注释便于理解；经核对 `config.py` 上述键当前无读取方，原注释（如「JWT 至少 32 字节」）易误导 | 用户（本会话明确请求加注释） | 已完成 |
| 2026-09-06 | `docs/07_backend_design/api_spec.md` §6.1 / §7.0 API-001、API-002；`docs/10_ai_context/context/m0.md`；`docs/10_ai_context/ai_context.md` S-M0-01 | API-001 增加可选 query `prompt=login`（非法值 `HT-VAL-001`）；API-002 浏览器失败改为 302 回 SPA 并带 `oidc=failed`（表示 HT-AUTH-003），不把 ErrorEnvelope / code / state / token 放入失败跳转 URL。首次无会话仍走 OIDC；失败后全局壳恢复面再 `prompt=login` 回 IdP。不新增 API/页面/LDAP，不收集 HuntAI 密码 | 企业 SSO 失败后须在 IdP 输入公司账号密码再入系统；用户本会话确认方案 A Plan | 用户（本会话确认方案 A Plan） | 已完成 |
| 2026-09-04 | `.env.example` | 新增 `CI_LOG_CHUNK_BYTES`、`CI_LOG_MAX_TOTAL_BYTES`、`REPORT_PARSE_TIMEOUT_SECONDS`（空=不启用超时）、`REPORT_PARSE_BATCH_ROWS` 键名（无默认值） | S-M2-05 外部 CI 日志分片与超大报告分批解析配置；禁止硬编码 | 用户（S-M2-05 Plan） | 已完成 |
| 2026-09-03 | `integration_hub.project_ci_trigger_configs`（实现表） | Alembic 0020 每项目一行：`bindings` jsonb + `aggregate_version`；承接 API-167 CI 触发绑定；`data_model.md` 未列该表 | S-M2-03 切片需要持久化仓库/分支↔计划绑定；实现侧补表并登记 | 用户（S-M2-03 Plan） | 已完成 |
| 2026-09-03 | `.env.example` | 新增 `ARTIFACT_ROOT` 键名（无默认值）；S-M2-01 Playwright 制品本地卷根路径配置 | S-M2-01 切片需要 M0/M1 本地卷落盘 object_key；禁止硬编码路径 | 用户（本会话确认 S-M2-01 Plan） | 已完成 |
| 2026-09-03 | `test_assets.test_plans.schedule_binding`（实现列） | Alembic 0016 可空 jsonb，承接 API-055 定时绑定；cron/时区仍 TBD，禁止写默认频率 | S-M1-06 切片需要持久化 schedule 绑定；`data_model.md` §7.3 未列该列 | 用户（S-M1-06 Plan） | 已完成 |
| 2026-09-03 | `.env.example` | 新增 `JENKINS_WEBHOOK_SECRET`、`JENKINS_API_TOKEN` 键名（无默认值）；S-M1-05 external_ci Jenkins 验签与 API Token 引用 | S-M1-05 切片需要 Jenkins webhook HMAC 与 trigger Basic 认证 env 引用；禁止硬编码密钥 | 用户（S-M1-05 Plan） | 已完成 |
| 2026-09-03 | `results_evidence.failure_clusters.fixes`（实现列） | Alembic 0014 增加可空 jsonb `fixes` 列，用于 API-131 `fixes_preview`（A4 建议投影）；`data_model.md` §7.8 未列该列 | S-M1-03 实现需要持久化 A4 fixes；数据模型遗漏，实现侧补列并登记 | 用户（S-M1-03 会话） | 已完成 |
| 2026-09-02 | `.env.example` | 新增 `TEST_RUN_HEARTBEAT_TIMEOUT_SECONDS` 键名（无默认值）；S-M1-02 TestRun 心跳回收配置项 | S-M1-02 切片需要可选心跳超时配置；TBD 秒数禁止硬编码 | 用户（本会话确认 S-M1-02 Plan） | 已完成 |
| 2026-09-01 | 根 `AGENTS.md`（新会话起步表、§6、§7 第 20 条、§9）；`docs/00_setup/project_rules.md` §3.3、§4；`docs/00_setup/README.md`；`.cursor/rules/project-conventions.mdc`；新增 `.zcode/rules/project-conventions.md` | 新增「切片进度同步」工程规则：每个 `S-M*` 切片合入前必须回写 `docs/10_ai_context/ai_context.md` 状态列与 `context/mN.md`。权威正文只在 `AGENTS.md` / `project_rules.md`；Cursor 与 ZCode 规则目录仅短指针且必须同改。不新增业务规则、页面、对象、状态或 API 编号 | 切片代码已合入但 Stage 10 表仍标「未做」（S-M0-12）；用户本会话明确要求在 `.cursor` / `.zcode` 加规则，并接受以 AGENTS.md 为唯一正文、工具目录只作指针 | 用户（本会话明确请求） | 已完成 |
| 2026-08-31 | `docs/07_backend_design/data_model.md` §1.2、§2.1、§3.1、§5.5、§6、§7.1、§8、§10、§11、§16 | 在 `identity_tenancy` 增补传输/安全基础设施表 `auth_sessions` 与 `oidc_login_drafts`（非第 24 领域对象），承接 api_spec API-001–003 的可吊销服务端会话与 PKCE 服务端草稿。不新增领域对象、状态或 API 编号；会话时长 / Cookie 名 / SameSite / 再认证窗口仍为 TBD，模型不写默认秒数 | 契约要求「服务端会话记录」可吊销，原 §5 仅覆盖一致性机制（Outbox/Inbox/intent/幂等），未覆盖身份传输面；用户本会话裁定按基础设施表补入而非升格领域对象 | 用户（本会话明确「按基础设施表补进 data_model」） | 已完成 |
| 2026-08-31 | 根 `AGENTS.md`（完善版）；新增根 `CLAUDE.md`；`docs/00_setup/project_rules.md` §2 白名单与文首冲突规则、§6 R2 冻结判定；`docs/00_setup/README.md`；`.cursor/rules/project-conventions.mdc` | 在 Stage 0 初始版 `AGENTS.md` 上补充设计资产约束，生成完善版仓库级 Agent 指令（目标/目录、技术栈与架构边界、前后端与 API 鉴权错误码、数据层与敏感数据红线、UI 约束、测试运行部署、禁止行为、规范冲突处理）。`CLAUDE.md` 为 Claude Code 入口，正文以 `AGENTS.md` 为唯一权威。顶层白名单加入 `CLAUDE.md`。不新增业务规则、页面、对象、状态或 API 编号 | 用户本会话明确要求生成完善版 CLAUDE.md / AGENTS.md | 用户（本会话任务正文） | 已完成 |
| 2026-08-30 | `docs/08_prd/prd.md`、`docs/08_prd/README.md`；`docs/06_architecture_design/architecture.md` §16；`docs/06_architecture_design/frontend_design_spec-v1.0.md` 文首；`docs/06_architecture_design/frontend_backend_boundary_spec-v1.0.md` 文首；`docs/README.md` | PRD v2 → v2.1 上游一致性复审回写（五份上游文档交叉审计）：① 修正事实错误——ADR Accepted 集合漏 ADR 0009（同源错误在 architecture.md §16，一并修正）；② 修 6 处失效交叉引用（`PRD §6.2`、裸 `§2.3`、`架构 C1`、`api_spec §8`、`边界 §6` 引文出处）；③ 补追溯断链——第 6 章对象列对齐 problem_model §3 CRUD 矩阵，TestPlan / ModelRoute / ApiToken 吊销 / 连接器 CRUD / API-223 导出下载新增 AC-089–AC-096（AC 总数 88 → 96），P02/P20/P22/P23/P24/P25 六个 M0–M2 页面纳入主页面列；④ 补 C1 缺的 5 条异常分支并新建 C2/C3/C4 异常表；⑤ 登记 CONFLICT-5（`gate_waiver` 是否覆盖 Readiness 豁免：C1 交互链与边界规范 §2.7 不一致，不自行裁决）；⑥ §0 自检两项由 PASS 降级为 PARTIAL；⑦ 统一版本标注约定（文件名 `-v1.0` 为稳定标识，权威版本以文首「版本」字段为准）与「05」引用歧义；⑧ `docs/README.md` 修正已删除的 `05_prototype/prototype_spec.md` 失效链接。**不新增业务功能、页面、领域对象、状态或 API 编号；US-01…19 / FR-01…19 编号不变** | 用户要求 review PRD 及其上游关联文档并优化修改；本次为一致性修复，逐条以上游文档为事实源 | 用户（本会话确认范围「全面修改」+ 深度「三层」+ TestPlan/ModelRoute「补验收」） | 已完成 |
| 2026-08-29 | `docs/08_prd/prd.md`、`docs/08_prd/README.md` | 中期 PRD v1.6 升级为 v2 综合交付版：US-01…19 / FR-01…19 编号不变；对齐建模 23 对象与状态机、C1–C4、前后端边界、P01–P25、API-xxx 验收挂钩；新建 AC-001…AC-088 可测验收；不发明新业务功能 | 用户确认本会话 PRD v2 综合交付 Plan（改已有结论，先 Plan 后实施） | 用户（本会话确认 Plan） | 已完成 |
| 2026-08-29 | 新增 `docs/06_architecture_design/tech_stack_decision-v1.0.md`、`docs/06_architecture_design/adr/0009_technology_stack_freeze.md`；修改 `docs/06_architecture_design/frontend_design_spec-v1.0.md` §1.3、`docs/06_architecture_design/architecture.md` §15.1、`docs/06_architecture_design/adr/README.md`、`docs/06_architecture_design/README.md` | 冻结八类技术栈（前端框架 / UI 组件 / 状态管理 / 后端框架 / ORM / API 风格 / 测试框架 / 构建部署）至锁定版本，附 License、维护活跃度与逐版本漏洞扫描结论。三处改动已有结论：① frontend_design_spec §1.3 路由库由「Stage 11 待定」定为 React Router 7.18.3（红线 R4 依赖批准）；② architecture.md §15.1 补 M0/M1 分期启用说明（Temporal / Redis / MinIO / Vault 推迟至 M2+，M0/M1 单 PostgreSQL 数据面）；③ adr/README.md 阅读规则放开 ADR 0009 可承载依赖与部署决策 | 用户要求确定并冻结技术栈，并要求每项可独立运维、高危 CVE 零未处置；分期启用与部署形态（前期 docker compose、后期 Kubernetes）、路由库选型均由用户本会话逐项裁定 | 用户（本会话逐项确认：分期启用 phased / 部署 compose→K8s / React Router / 保留 TanStack Query） | 已完成 |
| 2026-08-29 | `docs/03_problem_modeling/problem_model.md`；`docs/04_interaction_design/chains/c4_failure_triage.md`；`docs/06_architecture_design/`；`docs/08_prd/prd.md` | 按独立复审修订 Stage 6：接受模块化单体控制面与 Temporal 持久工作流选型；明确该选择不等于生产就绪，保留恢复、版本、容量、成本和运维 Gate；补齐 Temporal/Outbox/Activity 路由、模块拓扑、一致性矩阵及外部副作用 `unknown` 结果与 execution intent 恢复协议 | 用户已完成 Temporal 适配 POC，并明确要求以 2026-08-29 架构复审结果为准修订冻结资产 | 用户（本会话明确确认修订计划） | 已完成 |
| 2026-08-27 | `docs/06_architecture_design/architecture.md`；`docs/06_architecture_design/README.md` | `architecture.md` 从 Draft 转为 Stage 6 已定稿集成正文；README 同步登记该文件状态。8 份 ADR 仍 Proposed，00–04 分册与前端规范仍 Draft；不把 Proposed/TBD/POC 提升为已批准实现 | 用户确认架构文档符合要求并要求定稿 | 用户（本会话明确请求） | 已完成 |
| 2026-08-27 | `docs/00_setup/project_rules.md` §2、`AGENTS.md` §1；新增 `.cursor/` | 顶层仓库级配置白名单增加 `.cursor/`（Cursor MCP 与 rules）；与既有 `.zcode/`、`.mcp.json` 并列，不替换 | 在 Cursor 中使用与 ZCode 相同的 LangChain MCP，并明确 Cursor 工程配置合法落点 | 用户（本会话明确请求） | 已完成 |
| 2026-08-25 | docs/05_prototype/ 新增 5 份 UI/UX 模板 | 自源仓库 UI-UX-docs 的 docs/templates 复制 page-inventory 等五份模板（工具，非阶段产出），外链转源仓库限定 | 原型阶段需要统一的页面清单/组件清单/评审模板 | Tony | 已完成 |
| 2026-08-25 | docs/06_architecture_design/frontend_design_spec-v1.0.md（新增） | 生成前端设计规范 v1.0：P01–P25 页面清单、路由结构、跳转矩阵、前端职责边界、核心交互要求、响应式工程约定、与 Prototype 对应关系；缺口 6 项上报 | Stage 6 前端分册收口，供原型评审与实现使用 | Tony | 已完成 |
| 2026-08-24 | docs/01–08、13 新增 14 份设计文档 | 迁入 opensource-product-analysis 仓库决策文档集：按仓库规范重命名（snake_case）、改写全部内部交叉链接、外部资产（competitors/ references/）转为源仓库限定引用；各阶段 README 登记产出 | 复用已完成的设计资产，避免重复调研与建模 | Tony | 已完成 |
|  |  |  |  |  |  |
