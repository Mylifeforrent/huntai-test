# 06 · 核心交互链设计（Stage 6 · ✅ 已生成 v1.1）

> - **状态**：已生成 v1.1（2026-08-24）——四条链路规范文档位于 [chains/](chains/) 目录。**Stage 6.5 缺口回写**（同日）：26 项缺口裁定并回写上游（05 → v1.2、12-PRD → v1.4、README 分期注记 + 7.10 对齐）。**一致性复核消费**（同日，见 [consistency_fixes_20260824.md](../13_changes/consistency_fixes_20260824.md) 后续建议 #1）：下游口径对齐 05 / 12-PRD 一致性修复版——页面以 [05 §4.2](../03_problem_modeling/problem_model.md) **25 页**清单为唯一事实源（作废「20 个页面」）、领域对象以 05 §1.1 **23 个**为准；数值口径类（审批 TTL / 心跳周期 / 各超时阈值）仍统一移交 Stage 8 后端架构配置项表。一致性检查：页面 ⊆ 05 §4.2、状态与跳变 ⊆ 05 §2、FR 溯源 ⊆ 12-PRD 冻结编号，全部 PASS
> - **上游输入**：[05-业务建模](../03_problem_modeling/problem_model.md)（领域对象 23 + 状态机全集 + 25 页 IA）· [12-PRD](../08_prd/prd.md)（用户故事 / FR / 验收标准）· [README](../README.md) 第 1 章（北极星闭环与产品原则）——上游版本以各文首版本头为准
> - **下游消费者**：[07-产品原型设计](../05_prototype/prototype_spec.md)（Stage 7）· [12-PRD](../08_prd/prd.md) v2 终版

## 1. 必须覆盖的四条核心链路（来自既有决策，不得增删）

| # | 链路 | 依据 |
| --- | --- | --- |
| C1 | 北极星质量闭环：Confluence 需求 → AI 生成（两步式）→ 审阅入库 → 执行（双模式 × 双环境）→ 质量门禁 / GitHub Check Run → Jira 缺陷 → Release Task（Readiness Gate → Release 系统）→ 证据回流 | README 第 1 章、FR-05/13/14/15/18 |
| C2 | 审批链：动作发起 → 参数哈希 → 审批中心（九要素卡片）→ 执行前复核（anti-TOCTOU）→ 执行留痕 | FR-04、README 9.12 |
| C3 | 执行发起链：模式选择（Script / Agent）→ 环境选择（平台执行器 / 外部 CI）→ 参数表单（按 Job Schema 动态生成）→ TestRun 状态机流转 → 结果与证据 | README 4.0、FR-08/18/19 |
| C4 | 失败分诊链：TestRun 完成 → 报告归一化 → 失败聚类（含阻塞判断）→ 人工审阅修正 → 建缺陷 / 自愈建议 → 证据落库 | FR-06/07 |

## 1.1 生成结果索引（v1.1）

| 链路 | 文件路径 | 覆盖 FR（主） | 缺口上报 |
| --- | --- | --- | --- |
| C1 北极星质量闭环 | [chains/c1_north_star_quality_loop.md](chains/c1_north_star_quality_loop.md) | FR-05/13/14/15/18 | 5 项 |
| C2 审批链 | [chains/c2_approval_chain.md](chains/c2_approval_chain.md) | FR-04（+ README 9.12） | 8 项 |
| C3 执行发起链 | [chains/c3_execution_kickoff.md](chains/c3_execution_kickoff.md) | FR-08/18/19 | 9 项 |
| C4 失败分诊链 | [chains/c4_failure_triage.md](chains/c4_failure_triage.md) | FR-06/07 | 4 项 |

**口径对齐（v1.1）**：C1–C4 收口纪律统一为「页面 ⊆ 05 §4.2（**25 页**，不得引用过时的『20 个页面』）；领域对象 ⊆ 05 §1.1（**23 个**）；状态 ⊆ 05 §2」。链路正文行为语义未改，仅消费上游一致性修复后的数量与版本引用。

**建议阅读顺序**：C1（端到端主链，建立全局视角）→ C3（执行发起与 TestRun 状态机全路径）→ C4（失败分诊，承接 C3 终态）→ C2（审批链，横切 C1/C3/C4 全部 L2+ 副作用点）。C1 中的审批 / 执行 / 分诊环节均以相对链接指向 C2/C3/C4，可按需跳转。

## 2. 每条链路的输出结构要求

| 小节 | 内容 |
| --- | --- |
| 触发者与前置条件 | 谁在什么角色 / 状态 / 权限下进入链路 |
| 页面流 | 经过哪些页面（必须引用 05 §4 页面清单编号，不得新造页面） |
| 状态迁移 | 涉及的状态机跳转（引用 05 §2 编号；**不得新增未定义状态**） |
| 分支与异常 | 取消 / 超时 / 降级 / 审批拒绝 / 参数失效 / 僵尸回收等分支走向 |
| 审批与副作用点 | 哪些动作触发 L2+ 审批（对照副作用分级） |
| 证据落点 | 各环节产生哪些 EvidenceObject / AuditEvent |

## 3. 生成纪律

1. 不引入 05 / 12-PRD 之外的新功能、新页面、新状态；发现缺口先回写上游文档再入链；
2. 每条链路以 Mermaid sequenceDiagram 或 flowchart 表达，配页面流表格；
3. 完成后将头部状态改为「已生成 vX.0」并记录变更。

## 4. 变更记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-08-24 | Stage 6 首版：C1–C4 四条链路规范生成；Stage 6.5 26 项缺口回写上游 |
| v1.1 | 2026-08-24 | 消费 [consistency_fixes_20260824.md](../13_changes/consistency_fixes_20260824.md) 后续建议 #1：索引与 C1–C4 对齐 05「25 页 / 23 对象」及 12-PRD 一致性修复口径；作废「20 个页面 / 20 个领域对象」引用；无业务行为变更 |

> **版本引用约定**：文档间相互引用时，以**被引方文首版本头**为唯一事实源，不在引用处硬编码版本号（本文引用上游 05 / 12-PRD / README 同理）；任一文档升版无需回头同步引用处。本版（v1.1）对齐时点上游快照：05 v1.3 / 12-PRD v1.5 / README v1.5。**例外**：特性归属类版本标注（如「05 §2.3 v1.2 冻结表」「回写记录」、本文变更记录）为历史溯源陈述，记录功能随哪个上游版本落地，不受此约定约束。
