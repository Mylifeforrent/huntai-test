# 07_backend_design — 数据模型与 API 规范

存放数据模型与 API 规范产出：ER 模型与表结构、API 契约（路径 / 方法 / 请求响应 / 错误码）、AI 编排层（LangChain）接口约定。
由 Stage 7（数据模型与 API 规范）产出。

**约定输出**：`data_model.md` + `api_spec.md`（Markdown，API 以 OpenAPI 风格描述）
