# M0 实现提示 · 基座

权威：`docs/08_prd/prd.md` US-11/12/15/16/19；附录 B.10 M0 Gate。切片清单与状态列见 [ai_context.md](../ai_context.md) §4.1（截至 2026-09-01）。

## 硬约束

- 只实现 `api_spec.md` 已编号端点。ErrorEnvelope；无会话 **401 HT-AUTH-001**，禁止伪装 404。
- 无 JIT：callback 必须命中已有 `users.idp_subject`；0 或 >1 行 → 无组织上下文拒绝。
- LDAP 不做。Cookie 名/TTL/再认证窗口秒数仍 TBD，只经 `.env` 注入，禁止在代码写默认产品秒数。
- M0/M1 禁止启用 Temporal / Redis / MinIO / Vault。TestRun「无永久 running」用 PostgreSQL 状态 + 行锁/定时扫描，不要借机引入 Worker 产品。
- 身份模块禁止 import 审计 ORM；只走 `audit_port`。
- 前端：无登录页（P01–P25 无此项）；首次无会话 `HT-AUTH-001` 跳 OIDC（不加 `prompt`）；OIDC 失败由 API-002 302 回 SPA 并带 `oidc=failed`，全局壳恢复面禁止自动重跳，用户确认后 `API-001?prompt=login` 再进 IdP 账号密码表单。viewer 只读为 UX。HuntAI 不收集密码。

## 切片要点

**S-M0-01（部分完成）**  
已有 API-001–006、010 与 SessionGate；本地 mock IdP + `idp_subject` 种子。2026-09-06：API-001 可选 `prompt=login`；API-002 失败 302 + `oidc=failed`；全局壳 SSO 恢复面；mock IdP 在 SSO 未建立或 `prompt=login` 时出示合成账号表单（非产品登录页）。残留：全请求租户中间件/ORM 过滤；API-006 过期提示接入 UI（可选）；生产 IdP 替换 mock。AC-001/002。LDAP 仍不做。

**S-M0-02（完成）**  
P02/P04 接 API-011–016。项目为 Jira 只读镜像，**禁止**产品面「手工建项目」。角色四值。跨租户统一不存在。

**S-M0-03（完成）**  
API-170–172 落在 `integration_hub.api_tokens`（与 architecture / data_model 一致，非 identity schema）。argon2 哈希；明文仅 API-171 首次响应；幂等重放不回发明文。空 `project_ids` → `HT-VAL-001`；不可感知项目 → `HT-RES-001`。tester/viewer → `HT-IAM-001`。M0 边界：无 API-080；签发限流窗口 TBD 故未实现 `HT-QUOTA-002`；`last_used_at` 未异步更新。

**S-M0-04（完成）+ S-M0-05（完成）**  
Gate 四值（API-120）与审批队列（API-110–113、121）已接线。九要素卡片；参数哈希绑定；四眼；`APPROVED` ≠ `EXECUTED` + `ok`。unknown 进对账。C2。

**S-M0-06（完成）**  
LLM 唯一出口；无旁路。ModelRoute 分级与成本上限。不展示 Prompt 原文/token 明文。AC-089。P22 接 API-184/185/196–198。

**S-M0-07 / 08（完成）**  
OrgQuota（API-017/018/183）；超限拒绝新 AI。P23：关停 L1 即时、恢复 L3 + `kill_switch_restore`（API-199）。横幅以后端 `capability_controls` 为准。

**S-M0-09（完成）**  
API-024 审计检索、API-025 详情。SIEM 只投影已启用/未启用（API-040），无内部实现细节。P24 已接真数据。

**S-M0-10（完成）**  
对齐 B.10「执行环境注册中心」：注册、健康、Job 发现、params schema（API-100–105、070、102、103）。P15 已接。API-106 已由 S-M0-12 在 `integration_hub` 落地（`credential_ref` 仅 `env:` 引用）。不要在本切片做 Jenkins 触发（那是 S-M1-05）。

**S-M0-11（完成）**  
API-020 后端聚合四卡；经 query_port 读审批 / TestRun / 配额，禁止前端自己数「待审批」。WAITING_* 带 `dwell_seconds`。M0 边界：`gate_anomalies` 恒 `[]`（无 GateEvaluation 表）；`is_expiring` 阈值 TBD 故省略；切片表上的 API-022 通知角标属前端缺口 G4，本切片未做。P01 仍另调 API-017 作配额佐证。

**S-M0-12（完成）**  
HMAC 入站 API-090；验签失败 `401 HT-AUTH-004`，失败观察不得抢占后续合法投递的幂等键。连接器只读 API-160/161/164；API-106 绑定 `env:` 引用。M0 边界：无连接器创建 API；写 Jira / Check Run 等到 M2。

**S-M0-13（完成）**  
`run_orchestration`：TestRun 10 态入库、API-060–063 发起/取消、幂等与 CAS；心跳回收走 PG 行锁。P08/P09 已接列表/详情/发起。M0 边界：发起后保持 `PENDING`（无执行器/Jenkins）；无 SSE（API-210）、用例结果（064）、聚类（130）；`preview_id` / `approval_request_id` 只收 UUID、不参与授权。不要把 Temporal 当 M0 运行时。
