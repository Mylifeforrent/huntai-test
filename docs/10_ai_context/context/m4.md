# M4 实现提示 · 愿景（默认不进入当前 Stage 11 迭代）

权威：US-13 后半、PRD §8.3 / 附录 B.10 M4。清单见 [ai_context.md](../ai_context.md) §4.5。

## 默认不做

未单独批准前，禁止实现或「顺便加上」：

- P19 技能管理、MCP SDK、LangGraph（M0–M2 明确禁止，M4 才讨论 Agent 完整形态）
- Copilot 写操作（`copilot_write`）、NL2Script、RAG 知识回流、Prompt A/B、LLM-as-a-Judge 产品化
- 把 Agent 结果送进门禁或 Release 证据

## 若将来开切片 S-M4-01

- 仍不得新增领域对象/API 编号；先走 `docs/13_changes/change_log.md` 若需改冻结资产。
- 轨迹转脚本：人工确认后才是 TestCase DRAFT，再走 FR-05 评审，不得直写 ACTIVE。
- 写操作可回滚、可审计；越权测试集为放行 Gate。
