# M1 实现提示 · 接口自动化与引用型 v1

权威：US-01/02/03/14（M1 段）/17；FR-05–08、FR-18 M1。依赖：**M0 切片完成**（尤其 S-M0-04/05/06/13）。清单见 [ai_context.md](../ai_context.md) §4.2。

## 硬约束

- 生成默认**不落库**；保存才 TestCase DRAFT + `ai-generated`。禁止 save 绕过审阅直写 ACTIVE。
- 所有 A1/A2 走 LLM 工厂 + AIInvocationLog（S-M0-06）。
- 变量解析失败必须报错，禁止静默保留原文。
- 受理回执 ≠ TestRun 终态。SSE（API-210/211）只推进度，不是命令通道。
- 引用型用例重放不得重复触发外部 Job（幂等键）。
- 仍禁止 Temporal/MinIO/Redis/Vault。

## 切片要点

**S-M1-01 FR-05**（**完成**）  
P07/P05。`test_assets` 模块：`test_cases` / `test_case_versions` / `import_sources` + 幂等；`ai_governance.a1_generations` 短表。API-030–035、204–205、180–182、211。A1 经 `llm_factory.invoke`（`task_type=general`，M0 degraded stub）+ 确定性 JSON 解析（OpenAPI/Postman/curl）；默认 ModelRoute 含 Confidential；**无 YAML**（`HT-VAL-003`）；`BackgroundTasks` + 新 DB 会话，无 Temporal。生成默认不落库；API-032 保存 DRAFT + `ai-generated`；采纳另记 `a1.adopt.v1` 日志。partial `failed_items` 必显；SSE 仅展示。Excel API-200/202 仍未做（非本切片）。TTL 产品默认未设。

**S-M1-02 FR-08**（**完成**）  
P08/P09。`BackgroundTasks` + 新 DB 会话调度：`PENDING→VALIDATING→RUNNING→SUCCEEDED/FAILED`（`script×platform_executor`，httpx 进程内执行，写 `case_results`/`step_runs`）；`external_ci`/`agent` 校验后 **不派发 RUNNING**（`result_summary.dispatch=deferred`）。变量 `{{}}`/`${func}` 未解析 → `FAILED`。API-062/080 受理回执持久化 `command_receipts`（API-071）；幂等重放不重复入队。API-069 执行选项；API-064–066 用例结果；API-210 SSE 仅展示。Tok `execute` 走 API-080；`read` 可读 060/061/064/065。M1 残留：Jenkins 触发 S-M1-05；Agent Worker S-M2-07；`TEST_RUN_HEARTBEAT_TIMEOUT_SECONDS` 秒数 TBD（未配置则不启周期回收）；`${func}` 无目录恒失败；`CaseResult.outcome` 用未冻结 `passed/failed/incomplete`；`external_ci` 校验通过后停留非 RUNNING（无 WAITING_EXTERNAL）。

**S-M1-03 FR-06**（**完成**）  
P09/P13。`failure_clusters` 表（0014）+ A2 `a2_service`（invoke `prompt/failure-triage@1.0.0`、规则 fallback confidence 0.3、`AIInvocationLog.result=degraded`）+ `cluster_service` API-130/131（`FailureClusterReport` 投影 `generation_status/degraded/unclustered_refs`）。终态 TestRun 有 `failed` CaseResult 时 `schedule_failure_triage`（同进程 await 新 DB 会话，无 `create_task`、无 Temporal）；幂等不重插簇。heal_apply：Preview 校验 confidence≥0.7（`HT-POL-001`）、`card_payload.rollback.capability=snapshot`；批准走 `test_assets.command_port.apply_heal_after_approval`（新 Version + CAS，失败 `EXECUTED+failed`）；API-039 回滚指针（owner/admin）。前端 P09 解析 API-130 报告、降级 banner、unclustered_refs 链接、ClusterCard「可应用」POST API-120。残留：locator_stale 仅分类展示（S-M1-04 交付 API-132/133 与 evidence_objects）。

**S-M1-04 FR-07**（**完成**）  
报告挂 TestRun（`failure_clusters.test_run_id` + `result_summary.clustering` 投影，API-061 不变）；`evidence_objects` 表（0015）+ 分诊前按失败 CaseResult 插入引用、`evidence_pool` 喂 A2、簇 `evidence_refs` 并集 CaseResult 证据 ID。API-132 PATCH 人工修正（`correction_history` append-only、`failure_cluster.correct` 幂等 + 审计）；API-133 GET 相似失败（同项目同 category 确定性 score 1.0/0.7）。P09：pending banner、修正历史、相似列表、证据 ID 文本、API-132 接线；heal_apply 路径不变。残留：P20/API-026–028/223 全量检索、MinIO 二进制制品、AC-033/035 解析评测与流式、A4 完整建议、locator_stale 仅展示。

**S-M1-05 FR-18 M1**  
Jenkins 触发 + JUnit。HMAC 验签无例外。与平台自建执行同一 TestRun 状态机。

**S-M1-06**  
P06 TestPlan。发布 Readiness 仍属 M3；本切片只让计划成为可引用证据源（AC-094/095）。

**S-M1-07**  
P11 策略可写。评估结论「阻断 CI」留到 M2（S-M2-03/08）。缺配不得默认 pass。
