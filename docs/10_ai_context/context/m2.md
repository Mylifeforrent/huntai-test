# M2 实现提示 · Web、门禁阻断、集成、Agent 试点

权威：US-04–07、US-13（试点）、US-14（M2 段）、US-06。M2 与 M3 可并行；**Release v1 依赖本里程碑 Jira**。清单见 [ai_context.md](../ai_context.md) §4.3。

## 硬约束

- Playwright **独立于 API 进程**。Agent 结果**不进门禁**、不作发布证据。
- 定位器无自动改写路径；落地走 diff → FR-04 → 快照 → 可回滚。
- 门禁 FAIL 必须影响 CI exit code（反面清单：竞品「只报告不阻断」）。
- 制品正文：M0/M1 本地卷 `object_key` 约定在 M2 仍稳定；**不要**本切片启用 MinIO 产品面（ADR 0009：MinIO 为 M2+ **且**须另走启用前置 + change_log）。
- Agent 终止信号必须服务端持久化（多 worker 可停）——可用 PG，仍不必上 Temporal，除非已单独通过生产 Gate（Q3）。

## 切片要点

**S-M2-01 FR-09**  
Playwright 子进程 `app.modules.run_orchestration.playwright_worker`（控制面始终 `create_subprocess_exec`，不在 API 进程 in-process 调 worker；pytest 子进程继承 `PLAYWRIGHT_WORKER_STUB=1` 免浏览器）。`results_evidence.artifacts`（Alembic 0018）+ `ARTIFACT_ROOT` 本地卷 `object_store.py`；API-220/221 会话代理（Restricted→HT-POL-001；无 presign/302）。失败 web 步骤 `observation_ref`=trace `object_key`；API-065 `artifact_ids`、API-031 `evidence_preview`、P09 选行→065/066→EvidenceViewer blob；P13 走 preview→220/221。`playwright==1.62.0`（PyPI 最接近 1.62.1）。残留：无 MinIO；无 Playwright Inspector / trace.playwright.dev；AC-096 证据导出属 S-M2-06。

**S-M2-02 FR-10**  
快照 `locator_health` 在 web 用例 `_build_snapshot` 从 steps 提取（`health=unknown` 落库）；API-031 GET 对空 `locator_health` 从 steps 派生并对齐最新 `locator_stale` FailureCluster（`results_evidence.query_port.get_latest_locator_stale_cluster_for_test_case`）投影 `health` ok/stale/unknown + A3 extras（`candidates`、`fix_preview`、`human_repair_hint` 等，非冻结枚举）。A2 规则路径对 failed web click/fill+selector 归类 `locator_stale`；`a3_service.run_a3_for_locator_cluster` 在 `run_failure_triage_background` 内 A2 之后顺序 await，fixes 以 `field=locator_health` + JSON `suggested` patch 写入 FailureCluster（`can_auto_apply=false`）；stub LLM degrade 时不产出 confidence≥0.7 修复。API-037/038 版本列表与快照；P13 `heal_apply`（API-120）+ API-039 回滚到上一版本（非当前指针）；审批后 `apply_heal_after_approval` 新 TestCaseVersion。`query_port` 只回 dict 投影，不把 FailureCluster ORM 借给 test_assets。残留：A3 仍为 factory stub（非真实视觉模型）；无 MinIO；health 码为 GET 投影而非冻结枚举。

**S-M2-03 FR-13**  
Alembic 0019 `gate_evaluations` + 0020 `integration_hub.project_ci_trigger_configs`（change_log 登记）。终态 script/external_ci 后 `schedule_gate_evaluation`（`quality_gates.command_port`，顺序 await，无 create_task）→ pass/fail 评估 + Check Run stub 三段式（`check_run_stub.sync_check_run_stub`，失败 bounded retry + `check_run.sync_failed` 审计，不改 result）。API-144/145/146；`gate_waiver`（API-120/112，owner/admin，confirm:true，CAS `waiver_approval_id`，不改 result）。API-167 PUT ci-trigger-bindings；API-162/163 连接器注册/更新（无 secret 泄漏）。P12 历史/明细/豁免；P03 API-167 正确 body。跨模块只走 query/command port（不借 TestRun/CaseResult ORM）。API-146 无评估时 `unevaluated_reason` 必填（含进行中 `not_terminal`）。残留：无真实 GitHub HTTP；p95/error_rate 固定 `not_measured`（S-M2-08）；CONFLICT-5 未裁决（gate_waiver 仅 GateEvaluation）；`result=waived` 过滤靠 `waiver_approval_id`，库内 `result` 保持 pass/fail；P25 无 162/163 表单（本切片后端最小）；API-167 无 GET，P03 刷新后 CAS 需用户重填 version。API-165/166 出站渠道配置已落地（`outbound_channels` jsonb；配置成功 ≠ 投递；无通知投递/G4）。

**S-M2-04 FR-14**  
`jira_write`（API-120/112）从终态 FAILED TestRun 的 FailureCluster/CaseResult 发起；`_validate_jira_write_preview` 校验目标、证据 ID、jira_project（或项目 `jira_project_key` 回退）、描述/复现非空；`param_hash` 冻结规范化 payload（含自动附着的 `evidence_ids`）。审批通过后 `integration_hub.command_port.execute_jira_write_after_approval` → `jira_write_stub`（无 HTTP；`external_request_id=sha256(org:approval:bound_hash)`；issue `HT-{short}`；bounded retry + `jira_write.sync_failed` 审计）。成功写 append-only `EvidenceObject`（`source_object.connector=jira`）+ `jira_issue` 投影于 API-130/131；TestRun 保持 FAILED。P09 ClusterCard「一键创建 Jira 缺陷」→ API-120 → 审批中心；P25 仅展示连接器健康/出站开关（无写 Jira 表单）。AC-063/064 覆盖 `test_api_jira_write.py`；`test_api_120_happy_l2_jira_write` 已改为真实 FAILED 簇种子。幂等对账按 JSONB `connector`/`external_request_id` 过滤。残留：无真实 Jira REST；case_result 目标前端入口未单独加按钮（后端已支持）。

**S-M2-05 FR-18 M2**  
`report_adapters.py`（junit/allure/playwright/pytest + `ReportParseError`；allure 逐 JSON 文件解析、禁止拼接后当单文件）。`external_ci_executor`：`collect_state` CAS；多路径报告采集 + 本地卷确定性 `object_key`（`kind=report|log`）；`chunk_key=report:{checksum[:16]}:{case_id}` 分批入库；Jenkins `progressiveText` 按 offset 增量、单片切块不跳字节，超限显式 `log_truncated`；唯一约束竞争用 SAVEPOINT（`begin_nested`），禁止裸 IntegrityError 污染事务。轮询观测 `record_poll_observation(source=poll)`；注册侧 `report_adapter` 枚举校验 HT-VAL-001。Alembic 0021 为空修订（partial unique 已在 0013）。API-210 SSE `hint=report_parsing`；P15 适配器下拉 / P25 投递列 / P09 解析进度+partial+external_ci 徽标。残留：无 MinIO；allure 路径须 manifest/collect_config 显式声明；真实 Jenkins E2E；artifact 仍整文件载入内存（未做边下边解析）。

**S-M2-06**  
`evidence_service.py`：API-026/027 证据检索/详情（游标 `created_at+id` 分页、`subject_type+subject_id` 成对校验 HT-VAL-001；主体经 case_result/failure_cluster/test_run → run → project 解析后验项目四角色，无法解析归属按组织级证据仅 owner/admin，fail-close 404）。API-028 受理 + `BackgroundTasks` 打包（幂等键复用 `results_evidence.command_idempotency_records`；Restricted 任一条整单 `HT-POL-001`，受理与打包双重拦截；打包回执 `accepted→running→succeeded/failed`；包内容仅证据元数据 claim/source_object/引用，zip=manifest+逐条 md，json/md 单文件；不内嵌制品二进制）。回执走 `run_orchestration.command_port.create/update` 与 `query_port.get_command_receipt_pointer`（dict，不借 ORM）；API-212 回执 SSE（progress/resource_changed 帧，Sess only，完成以 GET 回执 + API-223 为准）；API-223 每次重验 tester+ + `succeeded` + checksum（否则 HT-STATE-001），代理下载 + `export_package.download` 审计，`receipt_id` 非授权令牌。Alembic 0022：`artifacts.test_run_id` 改可空 + `source_receipt_id` 可空列（导出包 Artifact 按回执定位；API-220/221 对无 run 制品 fail-close 404）。P20 重写：契约内筛选进 URL（subject 成对/created_from/created_to/cursor），导出格式 zip/md/json，受理中 Badge + 回执轮询对账 + SSE 仅提示，完成后 API-223 blob 下载。viewer 导出/下载 HT-IAM-001（fail-close）。残留：无 MinIO/预签名（API-222 未实现）；组织级（无项目主体）证据不可导出（回执表 `project_id` 非空约束 + fail-close）；保留期/Legal Hold 策略 TBD 未实现；P20 无逐条勾选导出（仅主体范围整体导出）。

**S-M2-07 FR-19 M2**（**完成**）  
`agent_worker.py` 子进程（同 playwright_worker 模式）：executor agent 分支 VALIDATING 校验 `agent_manifest`（run params 声明 `allowed_tools/max_steps/total_timeout_seconds`，缺失→FAILED，不发明默认值）→ RUNNING → 执行。Tool Router 逐步：白名单外→DENY；`evaluate_policy_gate(agent_tool_action, declared_side_effect_level)`，未声明/L2+→DENY（不实现 agent_tool_action 审批恢复）；每步前查 `stop_signal_at`（API-063 消费端）；连续 3 次 DENY 自动终止（AC-085）；每步心跳。落库：A8 JSON→Artifact（`kind=agent_trajectory`，含 `executed_steps`/`meta.assertions` 供转稿）+ CaseResult/StepRun（action 只存 tool+args_hash，无参数原文）。终态：断言裁决 SUCCEEDED/FAILED；超步/超时→STOPPING→CANCELLED + `a8.status=incomplete`（AC-083）；用户终止→terminated；无自动重试。API-067（results_evidence，Sess 四角色）：脱敏投影（tool+args_hash），非 agent→`not_agent_source`。API-068（run_orchestration，tester+，幂等 `test_run.to_script_draft`，`expected_run_version` 可选 CAS）：仅 `completed` 轨迹可转（incomplete fail-close HT-STATE-001）；`request` 工具步骤→新 TestCase DRAFT（test_assets command_port，tags=ai-generated，原 run 不变，AC-087）；schema 失败 HT-VAL-004。P14：转稿成功跳转新用例。AC-086 沿用 S-M2-03 门禁排除（agent_source）；AC-084/088 沿用 API-063 信号持久化与心跳回收。残留：manifest 来自 run params（M4 SkillVersion 落地后切换）；非 request 工具不执行也不可转（试点工具集仅 request）；无真实 LLM Agent 运行时（确定性 worker）。

**S-M2-08 FR-12**（**完成**）  
`check_run_stub.sync_check_run_stub` 增加 `policy_mode`：pass→success；fail→`failure`(blocking)/`neutral`(report_only)——「仅报告 ⇄ 阻断」切换真实生效（GateEvaluation.result 仍如实记 fail，报告与阻断解耦）；`command_port._evaluate_run` 传 `policy.mode`。AC-061 纪律回归加固：skip 场景断言响应无 `not_evaluated`（结果枚举仅 pass/fail/waived）；AC-062 加固：策略 report_only→blocking 后历史评估行与 `check_run_ref.conclusion` 不变，新评估用新模式（fail→failure）；AC-058/059 改用 blocking 策略种子。P12 评估列表按 `policy_snapshot.mode` 显示「阻断/仅报告」徽标。门禁管线本就统一（无独立性能分支，AC-055 前提满足）。残留：p95/error_rate 仍 `not_measured=true`（US-18 后半 M3 接 PerfBaseline）；perf run 管线接入数据源留 M3；waived 结论的 Check Run 回写语义未定义（未实现）。
